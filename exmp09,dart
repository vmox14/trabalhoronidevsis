void main (){
  double salario=1433.00;
  double aumento=(salario+((salario*15)/100));
  double imposto=(aumento-((aumento*8)/100));
  print("Salário inicial: $salario, salário com aumento: $aumento, salário total com desconto de impostos: $imposto");
}
